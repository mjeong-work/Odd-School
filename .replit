modules = ["nodejs-20", "web"]
run = "npm run build && npm run preview -- --host 0.0.0.0 --port $PORT"

[nix]
channel = "stable-25_05"
packages = ["nano"]

[env]
PORT = "3000"

[[ports]]
localPort = 3000
externalPort = 80

[agent]
expertMode = true
